name: example

on: push

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: PWD
        run: pwd
      - name: LS
        run: ls
      - name: Checks2
        run: bash /home/runner/work/react-class/.github/workflows/deploy.sh