name: example

on: push

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: PWD
        run: pwd
      - name: LS
        run: ls
      - name: Create 1
        run: echo pwd > ci.sh
      - name: Create 2
        run: echo ls > ci1.sh
      - name: Checks1
        run: sh ci.sh
      - name: Checks2
        run: sh ci1.sh